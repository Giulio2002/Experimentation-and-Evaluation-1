// state the jar name and version, following x.0.0 with
// x being the assignment number
jar {
    archiveBaseName = 'assignment1'
    archiveVersion = '1.0.0'
}

application {
    mainClass.set("ch.usi.experimentation.assignment1.Main")
}